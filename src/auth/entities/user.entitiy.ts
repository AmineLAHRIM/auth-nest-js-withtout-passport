export class User {
  // Note: we're just putting this here, but ideally - this would be in it's own file - interfaces/user.ts
  username: string;
  password: string;
  firstName: string;
  lastName: string;
}
